# Dépendances minimales du Feature Updater (IA-3)
# Pas de ML (XGBoost/SHAP/scikit-learn) — ce service ne fait que
# lire Kafka et écrire dans Redis.

kafka-python==2.0.2
redis==5.0.4
python-dotenv==1.0.1
pyyaml==6.0.1
tenacity==8.3.0
